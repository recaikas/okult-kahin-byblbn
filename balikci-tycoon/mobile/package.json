{
  "name": "balikci-tycoon-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Balıkçı Tycoon — iOS ve Android kabuğu (Capacitor). Oyunun kendisi bir üst klasördedir.",
  "scripts": {
    "build": "node scripts/build-www.js",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios",
    "assets": "npx @capacitor/assets generate --ios --android --iconBackgroundColor '#1d6c8f' --iconBackgroundColorDark '#0d3f55' --splashBackgroundColor '#1d6c8f' --splashBackgroundColorDark '#0d3f55'"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
